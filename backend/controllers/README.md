# backend/controllers

Controllers implement request handling and business logic for routes.

Common patterns:

- Validate input, call model methods, send JSON responses.
- Each controller is grouped by resource (auth, recipes, pantry, favorites, detect).
