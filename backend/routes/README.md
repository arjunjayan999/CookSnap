# backend/routes

Express route definitions. Each file maps endpoints to controller handlers.

- `auth.js` — signup/login/oauth endpoints
- `recipes.js` — recipe CRUD and search
- `pantry.js` — pantry items management
- `favorites.js` — favorite recipes
- `detect.js` — image-detection proxy
