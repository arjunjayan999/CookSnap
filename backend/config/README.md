# backend/config

Configuration files for the backend service.

- **`db.js`**: MongoDB connection setup.
- **`passport.js`**: Passport strategies and auth helpers.

Tips:

- Keep sensitive values in `backend/.env`.
