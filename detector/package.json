{
  "name": "detector",
  "version": "0.1.0",
  "private": true,
  "description": "Convenience npm scripts for the detector (Python/uvicorn)",
  "scripts": {
    "install": "python -m venv .venv && .\\.venv\\Scripts\\python.exe -m pip install -r requirements.txt",
    "dev": ".\\.venv\\Scripts\\python.exe -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": ".\\.venv\\Scripts\\python.exe -m uvicorn main:app --host 0.0.0.0 --port 8000"
  }
}
