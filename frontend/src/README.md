# frontend/src

Main source folder for the React app.

Key folders:

- `components/` — UI components and skeletons
- `contexts/` — React contexts (Auth, Pantry)
- `lib/` — small utilities
- `pages/` — route-level pages
- `services/` — API wrappers
