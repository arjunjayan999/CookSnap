# frontend/src/components

Reusable React components used across the app.

Examples:

- `ImageUploader.jsx`, `RecipeCardSkeleton.jsx`, `PantryList.jsx`, `Header.jsx`.

Design: Basic composition of presentational and container components.
